# PostgreSQL (Docker)
POSTGRES_USER=study
POSTGRES_PASSWORD=study_change_me
POSTGRES_DB=studydb

# Host port (binds to 127.0.0.1 only)
PG_PORT=5432
PG_CONTAINER_NAME=pg-study

# dataset selector: datasets/<DBSET>/init
DBSET=ec-v0
